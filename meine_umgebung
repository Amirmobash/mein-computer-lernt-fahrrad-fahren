مشکل دقیقاً همونیه که تو عکس‌ها معلومه:

1. هنوز `flatbuffers` روی نسخه‌ی خیلی قدیمی **2018…** می‌افته و توش `import imp` داره ⇒ روی **Python 3.13** می‌ترکه.
2. TensorFlow 2.20.0 می‌گه باید **`protobuf>=5.28.0`** باشه، ولی یه جا `protobuf 4.25.8` نصب شده.
3. موقع اجرای `fahrrad_lernen.py` هم گفته **SciPy لازم است**.

✅ الان این دستورها رو **دقیقاً همین‌طوری** (داخل همون venv که `(meine_umgebung)` هست) بزن:

```bash
cd ~/fahrrad_projekt
source meine_umgebung/bin/activate
```

### 1) پاکسازی پکیج‌های مشکل‌دار

```bash
pip uninstall -y flatbuffers protobuf tensorflow keras
pip cache purge
```

### 2) نصب نسخه‌های درست (نکته مهم: flatbuffers باید <25 باشد تا اون نسخه‌ی 2018… انتخاب نشود)

```bash
pip install -U pip setuptools wheel

pip install --no-cache-dir "protobuf>=5.28.0,<6"
pip install --no-cache-dir "flatbuffers>=24.3.25,<25"

pip install --no-cache-dir "tensorflow==2.20.0"
pip install --no-cache-dir scipy pillow numpy
```

### 3) تست سریع (باید بدون Error باشد)

```bash
python3 -c "import tensorflow as tf; import flatbuffers, google.protobuf, scipy; print('TF:', tf.__version__); print('flatbuffers:', flatbuffers.__version__); print('protobuf:', google.protobuf.__version__); print('scipy:', scipy.__version__)"
```

اگر این دستور نسخه‌ها را چاپ کرد و ارور نداد، مرحله بعد:

### 4) اجرای آموزش

```bash
python3 fahrrad_lernen.py
```

**انتظار:** `Found ... images belonging to 2 classes` و بعد `Epoch 1/5 ...` و در آخر ساخت فایل:
`mein_fahrrad_modell.h5`

---

اگر باز هم همان خطای `imp` آمد، فقط خروجی این دستور را بده تا دقیق بگم کدام نسخه دوباره افتاده:

```bash
pip show flatbuffers protobuf tensorflow | sed -n '1,120p'
```
